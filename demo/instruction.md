Build a production-ready AI document processing backend service using Python and FastAPI. The service should support document upload, asynchronous processing, status tracking, and retrieval of extracted information through clean REST APIs. Use PostgreSQL to store document metadata and processing status, and integrate a suitable background processing mechanism for long-running document tasks. The code should follow clean backend development practices, with proper API validation, error handling, structured logging, environment-based configuration, and clear separation between API routes, business logic, database operations, and document processing components. The project should be easy to maintain and extend with future AI capabilities such as text extraction, chunking, embeddings, and LLM-based processing. All project files must be created under /app/document-processing-service/ using absolute paths only. The required output is a complete working project in /app/document-processing-service/ with the source code, configuration files, dependency definition, and a README at /app/document-processing-service/README.md describing the API contract, environment variables, and how to run the service. Do not include any explanation outside the generated project.